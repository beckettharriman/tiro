{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the panel and pill windows",
  "windows": ["panel", "pill"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
